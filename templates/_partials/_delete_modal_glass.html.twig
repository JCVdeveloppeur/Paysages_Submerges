

<div class="ps-modal" id="psDeleteModal" aria-hidden="true">
  <div class="ps-modal__backdrop" data-ps-close></div>

  <div class="ps-modal__dialog"
       role="dialog"
       aria-modal="true"
       aria-labelledby="psDeleteTitle"
       aria-describedby="psDeleteBody"
       tabindex="-1">

    <button class="ps-modal__close" type="button" aria-label="Fermer" data-ps-close>✕</button>

    <div class="ps-modal__header">
      <div class="ps-modal__kicker" id="psDeleteKicker">Suppression</div>
      <h2 class="ps-modal__title" id="psDeleteTitle">Supprimer ?</h2>
      <p class="ps-modal__subtitle" id="psDeleteSubtitle">Cette action sera irréversible.</p>
    </div>

    <div class="ps-modal__body" aria-live="polite">
      <p id="psDeleteBody">Confirmez la suppression.</p>
    </div>

    <div class="ps-modal__footer">
      <button class="ps-btn ps-btn--ghost" type="button" data-ps-close id="psDeleteCancel">
        Annuler
      </button>

      <form method="post" id="psDeleteForm" autocomplete="off">
        <input type="hidden" name="_token" id="psDeleteToken">
        <button class="ps-btn ps-btn--primary" type="submit" id="psDeleteConfirm">
          Oui, supprimer
        </button>
      </form>
    </div>

  </div>
</div>


