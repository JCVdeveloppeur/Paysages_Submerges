{% extends 'base.html.twig' %}

{% block title %}Administration des espèces{% endblock %}

{% block body %}
<div class="container my-5">

    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="mb-0">
            🐠 Fiches Espèces 
            <span class="badge bg-primary">{{ nombreEspeces }}</span>
        </h2>
        <a href="{{ path('espece_new') }}" class="btn btn-success">+ Ajouter une espèce</a>
    </div>

    {% if especes is empty %}
        <div class="alert alert-warning">Aucune espèce enregistrée pour le moment.</div>
    {% else %}
        <table class="table table-hover table-bordered align-middle">
            <thead class="table-light">
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Nom commun</th>
                    <th scope="col">Nom scientifique</th>
                    <th scope="col">Type</th>
                    <th scope="col">Biotope</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for espece in especes %}
                    <tr>
                        <td>{{ espece.id }}</td>
                        <td>{{ espece.nomCommun|default('—') }}</td>
                        <td><em>{{ espece.nomScientifique|default('—') }}</em></td>
                        <td>{{ espece.typeEspece|default('—') }}</td>
                        <td>{{ espece.biotope|default('—') }}</td>
                        <td>
                            <a href="{{ path('espece_edit', {id: espece.id}) }}" class="btn btn-sm btn-outline-primary">✏️ Modifier</a>
                            {{ include('espece/_delete_form.html.twig', { id: espece.id }) }}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endif %}

    <a href="{{ path('admin_dashboard') }}" class="btn btn-outline-primary mb-4">
        ⬅️ Retour au tableau de bord
    </a>

</div>
{% endblock %}


