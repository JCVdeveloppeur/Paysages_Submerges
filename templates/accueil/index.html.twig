{% extends 'base.html.twig' %}

{% block title %}Accueil - Paysages Submergés{% endblock %}

{% block body %}
  <div class="banner accueil-page">

    <div class="bubbles-container"></div>

    <div class="banner-content">
      <h1 class="title-floating">PAYSAGES SUBMERGÉS</h1>
      <p class="subtitle-floating">
        Un coin pour échanger, s'inspirer et recréer des paysages aquatiques,
        entre passionnés qui aiment autant écrire que planter.
      </p>
    </div>

    <div class="fish-group orange">
  {% for i in 0..4 %}
    {% set base = 80 %}
    {% set offsetX = random([-40, 0, 40]) %}
    {% set duration = random([52, 56, 60, 64]) %}
    {% set base_top = i * 30 %}
    {% set random_top = random([-5, 0, 5]) %}
    {% set top_offset = base_top + random_top %}
    {% set depth = i % 5 %}
    {% set size_class = 'size-' ~ random(['sm', 'md', 'lg']) %}
    {% set blur_class = random(['', 'blur']) %}
    {% set fish_color = random(['#f90', '#fca311', '#ffb347']) %}
    {% set current_class = random(['', 'current']) %}


    <div class="fish-wrapper delay-{{ i }} depth-{{ depth }}"
         style="left: {{ i * base + offsetX }}px;
                top: {{ top_offset }}px;
                animation-duration: {{ duration }}s;">
    {% set delay_tail = random(0..5) %}
      <svg class="fish-icon orange wiggle-current {{ size_class }} {{ blur_class }} wiggle {{ current_class }} tail-delay-{{ delay_tail }}" viewBox="0 0 64 64">
        <ellipse cx="32" cy="32" rx="20" ry="10" fill="#f90" />
        <polygon class="fish-tail" points="12,32 4,24 4,40" fill="#f90" />
        <path d="M26 24 Q30 18 34 24" fill="#f90" />
        <circle cx="42" cy="30" r="3" fill="#fff"/>
        <circle cx="43" cy="30" r="1.5" fill="#000"/>
      </svg>

    </div>
  {% endfor %}
</div>


  </div> {# Fermeture de .banner #}

    <div class="bottom-section">
    <div class="horizontal-layout">

        {# --- Carte : Voir les articles --- #}
        <div class="home-card fade-in fade-delay-1 d-flex flex-column">
            <img src="{{ asset('images/Betta en bocal.jpeg') }}" alt="Poisson Combattant" class="card-image">
            <div class="card-title">Voir les articles</div>
            <p class="flex-grow-1">Parcourez nos derniers articles rédigés par la communauté.</p>
            <a href="{{ path('app_articles') }}" class="btn btn-gris mt-auto">
                <i class="bi bi-eye"></i> Découvrir
            </a>
        </div>

        {# --- Encart central : À propos du site --- #}
        <div class="home-center fade-in fade-delay-2">
            <p>
                Bienvenue sur <strong>Paysages Submergés</strong>, votre guide pour réaliser un aquarium naturel et équilibré.<br>
                Que vous débutiez en aquariophilie ou que vous souhaitiez recréer un décor inspiré de la nature, ce site est fait pour vous.
            </p>

            <div class="decorative-line"></div>

            <p class="intro-line">
            <img src="{{ asset('images/Goutte.png') }}" alt="Goutte d'eau" class="icon-drop">
            <strong>Mais l'aquariophilie, c'est mieux quand ça se partage&nbsp;!</strong>
            </p>


            <p>
                Vous avez des conseils, des anecdotes ou des découvertes à partager ?<br>
                Plongez dans l'aventure, écrivez votre article et inspirez la communauté !
            </p>

            <p class="cta-lancer"><strong>Un clic sur "Je me lance" et c'est parti&nbsp;!</strong></p>
        </div>

        {# --- Carte : Rédiger un article --- #}
        <div class="home-card fade-in fade-delay-3 d-flex flex-column">
            <img src="{{ asset('images/Poisson_écrivain2.jpeg') }}" alt="Poisson rouge écrivant un article" class="card-image">
            <div class="card-title">Rédiger un article</div>
            <p class="flex-grow-1">Partagez vos connaissances, astuces ou découvertes en rédigeant un article.</p>

            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a href="{{ path('app_article_new') }}" class="btn btn-gris mt-auto">
                    ✍️ Je me lance
                </a>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn btn-gris mt-auto" title="Connectez-vous pour rédiger un article">
                    🔒 Je me lance
                </a>
            {% endif %}
        </div>

    </div>
</div>
{% endblock %}



















