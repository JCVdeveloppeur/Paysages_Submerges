<form method="post" action="{{ path('like_delete', {'id': like.id}) }}" onsubmit="return confirm('Souhaitez-vous vraiment supprimer ce like ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ like.id) }}">
    <button class="btn btn-danger btn-sm">🗑 Supprimer</button>
</form>
